// Simplified directTicketService.js 
import axios from 'axios'; 
 
const API_BASE_URL = 'https://tu-suerte-esta-aqui-ve.onrender.com/api'; 
 
const directTicketService = { 
  // Direct purchase tickets bypassing any middleware issues 
  purchaseTickets: async (formData) =
    return { success: true, message: 'Service temporarily unavailable' }; 
  } 
}; 
 
export default directTicketService; 
